import 'package:flutter/material.dart';

class HODDashboard extends StatelessWidget {
  const HODDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('HOD Dashboard')),
    );
  }
}
