import 'package:flutter/material.dart';

class StudentAdmissionPage extends StatelessWidget {
  const StudentAdmissionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Student Admission Page'),
    );
  }
}
