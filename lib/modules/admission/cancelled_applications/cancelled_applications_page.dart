import 'package:flutter/material.dart';

class CancelledApplicationsPage extends StatelessWidget {
  const CancelledApplicationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cancelled Applications Page'),
    );
  }
}
