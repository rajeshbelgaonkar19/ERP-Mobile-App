import 'package:flutter/material.dart';

class CAPApplicationPage extends StatelessWidget {
  const CAPApplicationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CAP Application Page'),
    );
  }
}
