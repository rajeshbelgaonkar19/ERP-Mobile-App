import 'package:flutter/material.dart';

class AdmissionDashboard extends StatelessWidget {
  const AdmissionDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Admission Dashboard Content'),
    );
  }
}

